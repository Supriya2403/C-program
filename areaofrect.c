#include<stdio.h>
int main()
{
    int l,b,area;
    printf(" enter the length:");
    scanf("\n %d ",&l);
    printf(" enter the breadth:");
    scanf("\n %d",&b);
    area=l*b;
    printf("\n  Area of rectangle is:%d ",area);
}